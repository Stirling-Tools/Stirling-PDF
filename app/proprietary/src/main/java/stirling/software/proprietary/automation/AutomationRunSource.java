package stirling.software.proprietary.automation;

/** Distinguishes the existing Automate tool from Processor's browser-side work for billing. */
public enum AutomationRunSource {
    AUTOMATE,
    PROCESSOR
}
