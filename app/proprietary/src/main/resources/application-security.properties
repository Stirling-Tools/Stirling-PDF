# Defaults for the `security` profile (proprietary + SaaS). Core never loads this.

# Policy subsystem on by default. Override with policies.enabled=false
# (settings.yml or the POLICIES_ENABLED env var).
policies.enabled=true
