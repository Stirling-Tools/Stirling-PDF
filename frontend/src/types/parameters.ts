// Base parameter interfaces for reusable patterns

export interface EmptyParameters {
  // Base interface for tools that don't require parameters
  // Provides extensibility for future parameter additions
}
