/**
 * Supabase client stub for desktop application.
 * Supabase has been removed — billing and auth are handled via backend API.
 * This stub exists for backward compatibility with any code that checks for supabase.
 */

export const supabase = null;
