/**
 * The prototypes sandbox has no @portal alias, so it must not resolve the
 * portal-authored docs view — shadow the seam back to the stub.
 */
export { default } from "@core/components/docs/DocsPage";
