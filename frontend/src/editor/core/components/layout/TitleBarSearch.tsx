/**
 * Renders Super Search into the title-bar strip when a layer provides one; the
 * default has no strip and renders nothing. A layer with a strip shadows this at
 * the same @app path.
 */
export function TitleBarSearch() {
  return null;
}
