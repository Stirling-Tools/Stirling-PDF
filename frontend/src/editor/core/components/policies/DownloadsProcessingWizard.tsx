/**
 * Core stub for the Downloads processing offer. The real implementation lives at the
 * same path under proprietary/ and shadows this via the @app/* alias cascade.
 */
export function DownloadsProcessingWizard(_props?: { active?: boolean }) {
  return null;
}
