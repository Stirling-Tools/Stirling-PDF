/**
 * Moved to {@code @app/components/shared/StepModalHeader} when the editor's checkout flow needed
 * the same stepped-modal chrome the portal flows wear. Re-exported here because eight portal and
 * SaaS flows import this path and the move is not worth churning them for.
 */
export { StepModalHeader } from "@app/components/shared/StepModalHeader";
