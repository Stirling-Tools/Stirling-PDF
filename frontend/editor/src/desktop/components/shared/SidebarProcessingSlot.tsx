/** Desktop offers Downloads processing in the first-run classification demo instead.
 * Shadows the proprietary slot, which would otherwise mount the Downloads wizard. */
export function SidebarProcessingSlot() {
  return null;
}
