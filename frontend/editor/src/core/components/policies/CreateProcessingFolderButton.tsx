export interface CreateProcessingFolderButtonProps {
  placement: "home" | "sidebar";
  collapsed?: boolean;
}

/** Builds with a processing service supply the creation entry point through this seam. */
export function CreateProcessingFolderButton(
  _props: CreateProcessingFolderButtonProps,
) {
  return null;
}
