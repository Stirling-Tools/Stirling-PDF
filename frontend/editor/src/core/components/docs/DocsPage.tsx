/**
 * Documentation browser. Core ships no docs manifest (it travels with the
 * portal chunk), so this stub renders nothing and HAS_DOCS keeps the route and
 * the rail entry out of the build.
 */
export default function DocsPage() {
  return null;
}
