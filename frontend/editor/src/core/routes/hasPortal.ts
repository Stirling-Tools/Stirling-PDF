/**
 * Whether this build ships the processor. Its own module, like PORTAL_BASENAME, so
 * core can ask without importing the route set and its portal references. Shadowed
 * per build.
 */
export const HAS_PORTAL = false;
