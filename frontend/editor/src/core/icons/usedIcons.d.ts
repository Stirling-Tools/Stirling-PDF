/** Shape of `virtual:used-icons`, scanned by usedIcons.vite.mjs when Storybook
 * starts. Registry names the app references, sorted. */
declare module "virtual:used-icons" {
  const usedIcons: string[];
  export default usedIcons;
}
