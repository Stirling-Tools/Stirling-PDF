import "@app/icons/colorless.css";

/**
 * Class that greys out a brand mark, for `<Icon colorless />`. Only meaningful on the marks in
 * svg/third-party: a stroke icon already follows currentColor, so greyscale finds nothing to drain.
 */
export const COLORLESS_CLASS = "icon-colorless";
