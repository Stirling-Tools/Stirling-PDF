/**
 * Mirrors the server's `PasswordPolicy.MIN_LENGTH`. A client check is a hint that keeps the
 * untranslated server rejection off screen, never the policy itself.
 */
export const MIN_PASSWORD_LENGTH = 8;
