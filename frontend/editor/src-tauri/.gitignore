# Generated by Cargo
# will have compiled files and executables — match nested target/ dirs too
# (provisioner/target/, thumbnail-handler/target/, ocr-setup/target/) since each
# sub-crate has its own Cargo workspace under src-tauri/.
target/
/gen/schemas
/runtime/

**/stirling_thumbnail_handler.dll
# Copied into windows/wix/ by scripts/build-provisioner.mjs, same as the two above.
**/stirling_ocr_setup.dll
