# Gradle daemon heap. The default (512 MiB) is exhausted by the larger builds
# (notably the saas flavor) under parallel execution, causing intermittent
# "JVM garbage collector is thrashing" daemon deaths in CI. 2 GiB heap + 1 GiB
# metaspace gives comfortable headroom on CI runners and typical dev machines.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 --enable-native-access=ALL-UNNAMED

# Enables parallel execution of tasks, allowing multiple tasks to run simultaneously
org.gradle.parallel=true

# Enables build caching to reuse outputs from previous builds for faster execution
org.gradle.caching=true

# allow Gradle to download the toolchain version requested by the build
org.gradle.java.installations.auto-download=true

org.gradle.daemon=true
# org.gradle.configuration-cache=true

# PDF-Elite Global Dependency Versions
# Standardized on Java 25
javaVersion=25

# Core dependency versions
guavaVersion=33.0.0-jre
commonsLang3=3.14.0
pdfboxVersion=3.0.8
jpdfiumVersion=1.0.1
bucket4jVersion=8.7.0
archunitVersion=1.2.1

# Missing version properties from audit
commonsIoVersion=2.15.1
bouncycastleVersion=1.78
rhinoVersion=1.9.1
commonmarkVersion=0.28.0
batikVersion=1.19
imageioVersion=3.13.1
googleJavaFormatVersion=1.22.0

# Proprietary Module Dependency Versions
springSecuritySamlVersion=6.2.3
gsonVersion=2.10.1
jackson2Version=2.16.1
jinjavaVersion=2.7.3
jwtVersion=0.12.5
awsSdkVersion=2.25.11
jschVersion=0.2.16
commonsNetVersion=3.10.0
smbjVersion=0.13.0
tinkVersion=1.12.0
testcontainersMinioVersion=1.19.7